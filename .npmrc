# Ensure consistent package-lock.json format across environments
lockfile-version=3

# Use exact versions to prevent unexpected updates
save-exact=true

# Prevent npm from modifying package-lock.json during install
package-lock=true

# Engine strict to enforce Node/npm versions (disabled for production compatibility)
# engine-strict=true
